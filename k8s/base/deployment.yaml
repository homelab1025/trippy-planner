apiVersion: apps/v1
kind: Deployment
metadata:
  name: trippy-planner
spec:
  selector:
    matchLabels:
      app: trippy-planner
  template:
    metadata:
      labels:
        app: trippy-planner
    spec:
      containers:
      - name: trippy-planner
        image: ghcr.io/homelab1025/trippy-planner-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
