apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trippy-backend
  labels:
    app: trippy-backend
spec:
  selector:
    matchLabels:
      app: trippy-backend
  endpoints:
    - port: metrics
      path: /actuator/prometheus
      interval: 30s
